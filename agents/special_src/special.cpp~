#include <iostream>
#include <map>
#include <string>

using namespace std;
int main()
{
   int n, i, c;
   
   cout << "Special Judge\n";
   n = 0;

   for (i=0; i<5; i++)
   {
     cin >> c;
     cout << "i=" << i << " c=" << c << endl;
     if (c!=2) {
         n = 1;
	 break;
     };
   };
   cout << "Special Judge Result " << n << endl;
   return n;
}
